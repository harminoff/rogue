cmake_minimum_required(VERSION 3.22.1)
project(roguetiles_android C)

# Task 7 will add the real Android native target with ROGUE_ANDROID,
# ROGUE_ANDROID_DEFAULT_ONLY, allegro_frontend.c, mobile_controls.c,
# and rogue_platform.c.
message(FATAL_ERROR "RogueTiles Android native sources are added in Task 7")
